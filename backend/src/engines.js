// Passed straight to Blender's -E flag, so these are its identifiers rather
// than names of our own - it has renamed EEVEE's twice across 4.x and 5.x.
export const ENGINES = ['CYCLES', 'BLENDER_EEVEE', 'BLENDER_WORKBENCH'];
